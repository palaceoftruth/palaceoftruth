{{- if eq (include "palaceoftruth.valkeySentinelEnabled" .) "true" }}
---
# Headless service required by the replica StatefulSet for stable pod DNS.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "palaceoftruth.valkeyReplicaName" . }}
  labels:
    {{- include "palaceoftruth.labels" . | nindent 4 }}
    {{- if .Values.valkey.metrics.enabled }}
    palaceoftruth.io/valkey-metrics: "true"
    {{- end }}
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: {{ include "palaceoftruth.valkeyReplicaName" . }}
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
    {{- if .Values.valkey.metrics.enabled }}
    - name: metrics
      port: {{ .Values.valkey.metrics.port }}
      targetPort: metrics
    {{- end }}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "palaceoftruth.valkeyReplicaName" . }}
  labels:
    {{- include "palaceoftruth.labels" . | nindent 4 }}
spec:
  replicas: {{ include "palaceoftruth.valkeyReplicaCount" . }}
  serviceName: {{ include "palaceoftruth.valkeyReplicaName" . }}
  selector:
    matchLabels:
      app: {{ include "palaceoftruth.valkeyReplicaName" . }}
  template:
    metadata:
      labels:
        app: {{ include "palaceoftruth.valkeyReplicaName" . }}
        {{- include "palaceoftruth.labels" . | nindent 8 }}
    spec:
      {{- if .Values.valkey.sentinel.primaryReplicaAntiAffinity.enabled }}
      # Keep every replica apart from the named primary. Operators must enable
      # this only where two eligible hostnames are available.
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels:
                  app: {{ include "palaceoftruth.valkeyPrimaryName" . }}
      {{- end }}
      initContainers:
        - name: init-config
          image: {{ .Values.valkey.image }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              # Reconcile the named replica back to the primary service after
              # a pod/IP recycle; stale Sentinel rewrites can point at dead pod IPs.
              if [ ! -f /data/valkey.conf ]; then
                echo "replicaof {{ include "palaceoftruth.valkeyPrimaryName" . }} 6379" > /data/valkey.conf
                echo "appendonly yes" >> /data/valkey.conf
                echo "appendfilename appendonly.aof" >> /data/valkey.conf
              else
                sed -i '/^replicaof /d;/^slaveof /d' /data/valkey.conf
                echo "replicaof {{ include "palaceoftruth.valkeyPrimaryName" . }} 6379" >> /data/valkey.conf
              fi
              {{- if eq (include "palaceoftruth.valkeyAuthEnabled" .) "true" }}
              # masterauth is what lets this replica finish its handshake with a
              # password-protected primary.
              {{- include "palaceoftruth.valkeyAuthConfigScript" "/data/valkey.conf" | nindent 14 }}
              {{- end }}
          {{- if eq (include "palaceoftruth.valkeyAuthEnabled" .) "true" }}
          env:
            {{- include "palaceoftruth.valkeyServerAuthEnvVars" . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: data
              mountPath: /data
      containers:
        - name: valkey
          image: {{ .Values.valkey.image }}
          command: ["valkey-server", "/data/valkey.conf"]
          {{- if eq (include "palaceoftruth.valkeyAuthEnabled" .) "true" }}
          # REDISCLI_AUTH lets the exec probes authenticate without exposing the
          # password in the probe command line.
          env:
            {{- include "palaceoftruth.valkeyServerAuthEnvVars" . | nindent 12 }}
          {{- end }}
          ports:
            - name: redis
              containerPort: 6379
          resources:
            {{- toYaml .Values.valkey.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
          readinessProbe:
            exec:
              command: ["valkey-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["valkey-cli", "ping"]
            initialDelaySeconds: 10
            periodSeconds: 10
        {{- if .Values.valkey.metrics.enabled }}
        {{- include "palaceoftruth.valkeyExporterContainer" (dict "root" . "address" "redis://127.0.0.1:6379" "auth" true) | nindent 8 }}
        {{- end }}
      {{- if and .Values.valkey.metrics.enabled (or .Values.valkey.metrics.existingSecret .Values.valkey.metrics.passwordFileKey) }}
      volumes:
        {{- include "palaceoftruth.valkeyExporterPasswordVolume" . | nindent 8 }}
      {{- end }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: {{ .Values.valkey.storage.size }}
{{- end }}
