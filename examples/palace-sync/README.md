# Palace Sync Sample

This directory is a small, public-safe folder sync corpus for local development.
Set `PALACE_SYNC_HOST_PATH` to a different directory when testing folder sync
against your own files.
